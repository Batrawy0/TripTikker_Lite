spring.application.name=external-api
logging.level.root=error
# = DATA SOURCE
# ===============================
# Set here configurations for the database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres?currentSchema=skytraveller
spring.datasource.username=postgres
spring.datasource.password=0002522
spring.datasource.driver-class-name=org.postgresql.Driver
# ===============================
# = JPA / HIBERNATE
# ===============================
# Show or not log for each sql query
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, update): with "create-drop" the database
# schema will be automatically created afresh for every start of application
spring.jpa.hibernate.ddl-auto=update
# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# ===============================

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Connection timeout
spring.data.redis.timeout=2000


# Amadeus API Credentials
amadeus.clientId=IerZ5fxld8KiLPiykGSA2hK9E5SNTwuw
amadeus.clientSecret=Fx6QASb8DVM2yJQG
amadeus.tokenUrl=https://test.api.amadeus.com/v1/security/oauth2/token

